generator client {
  provider = "prisma-client-js"
  output   = "../../src/generated/weally"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL_WEALLY")
}

model Meal {
  id    String @id @default(cuid())
  label String

  @@map("meals")
}
